apiVersion: 1

providers:
  - name: SmartInventory Dashboards
    orgId: 1
    folder: SmartInventory
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
